// chat window to hold gpt bot

function ChatWindow() {
    return (
        <div class="d-flex flex-column bd-highlight">
            <p> chat window </p>
        </div>
    );
}

export default ChatWindow;